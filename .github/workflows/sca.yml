name: SCA test
run-name: Hello Kyle

on:
  push:

jobs:
  sca:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Test
        uses: ./.github/actions/slsa
        inputs:
          security-patch-package-exclusion: "package1; echo 'malicious payload executed!'"