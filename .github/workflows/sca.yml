name: SCA test
run-name: Hello Kyle

on:
  push:

jobs:
  sca:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Node
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.1.0

      - name: Copy package.json to action directory
        run: |
          cp $GITHUB_WORKSPACE/package.json .package.json

      - name: Install
        shell: bash
        run: |
          npm install

      - name: Test
        uses: ./.github/actions/sbom
        with:
          docker-images-file: "docker-images.json\"; echo 'malicious payload executed!';\""